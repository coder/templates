name: Push templates

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'deployments/ai.coder.com/**'

permissions:
  contents: read

concurrency:
  group: push-templates-ai.coder.com
  cancel-in-progress: true

jobs:
  discover-templates:
    name: Discover templates
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.discover.outputs.matrix }}
    steps:
      - id: discover
        name: Discover changed templates
        uses: ./.github/actions/discover-templates
        with:
          deployment_dir: deployments/ai.coder.com

  validate-templates:
    name: Validate template
    needs: discover-templates
    runs-on: ubuntu-latest
    if: needs.discover-templates.outputs.matrix != ''
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.discover-templates.outputs.matrix) }}
    steps:
      - name: Validate template
        uses: ./.github/actions/validate-templates
        with:
          template: ${{ matrix.template }}
          org: ${{ matrix.org }}
          template_dir: ${{ matrix.dir }}

  push-templates:
    name: Push template
    needs: [discover-templates, validate-templates]
    runs-on: ubuntu-latest
    if: needs.discover-templates.outputs.matrix != ''
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.discover-templates.outputs.matrix) }}
    steps:
      - name: Push template
        uses: ./.github/actions/push-templates
        with:
          deployment_dir: deployments/ai.coder.com
          coder_url: https://ai.coder.com
          coder_session_token: ${{ secrets.CODER_SESSION_TOKEN_AI }}
          activate: "true"
          template: ${{ matrix.template }}
          org: ${{ matrix.org }}
          template_dir: ${{ matrix.dir }}
